import MainTitle from "../../headers/main/MainTitle";
import SearchBar from "../searchBar/SearchBar";
import { Wrapper } from "./SearchStyles";

const Search = () => {
  return (
    <Wrapper>
      <MainTitle />
      <SearchBar />
    </Wrapper>
  );
};

export default Search;
